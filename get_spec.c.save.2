#include "main.h"

/**
 * get_spec - dispatch one conversion specifier to its handler
 * @c: the specifier letter
 * @ap: variadic argument list
 * @b: output buffer
 * Return: characters printed, or -1 on error
 */
int get_spec(char c, va_list ap, buffer_t *b)
{
    int i;
    static const spec_t table[] = {
        { 'c', print_char },
        { 's', print_string },
        { '%', print_percent },
        { 'b', print_binary },     /* from task 3 */
        { 'u', print_unsigned },   /* task 4 */
        { 'o', print_octal },      /* task 4 */
        { 'x', print_hex },        /* task 4 */
        { 'X', print_HEX },        /* task 4 */
